// backend/config/emailConfig.js
// Nodemailer/email functionality has been removed from this project.
// This file is kept only to avoid breaking existing imports.

async function sendOrderConfirmationEmail() {
  // Intentionally left blank: emails are no longer sent.
  return;
}

module.exports = { sendOrderConfirmationEmail };
